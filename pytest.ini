[pytest]
# La racine du projet dans sys.path : permet `import webapp` / `import utils`
# dans les tests, que pytest soit lancé nu (CI) ou via python -m pytest.
pythonpath = .
# Par défaut, exécute aussi les tests du scraper (scraping/)
testpaths = tests scraping
