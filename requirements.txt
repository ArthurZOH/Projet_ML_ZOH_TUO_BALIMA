# Dépendances de base (dev/tests) -- l'étape "choix techniques" ajoutera
# les librairies de scraping, Deep Learning et interface (Requests/Selenium,
# TensorFlow, Streamlit ou Flask, etc.)
pytest==8.2.2

# Interface web (Étudiant C)
streamlit==1.46.0

# Scraping Jumia (Étudiant A)
requests==2.34.2
beautifulsoup4==4.15.0

# Épinglées : dépendances de streamlit (calcul + affichage images) dont les
# versions récentes ne fournissent plus de binaires Python 3.11. Versions
# compatibles 3.11 et TensorFlow (modèle Étudiant B).
numpy==1.26.4
pillow==10.4.0
pandas==2.2.3
pyarrow==18.1.0
charset-normalizer==3.3.2
